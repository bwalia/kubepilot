# Local-only secrets for the Mac Studio launchd agent.
# Copied to ~/.kubepilot/secrets.env once; never overwritten by deploy.
# DO NOT commit a filled-in copy.

KUBECONFIG=/Users/REPLACE/.kube/config
KUBEPILOT_DASHBOARD_AUTH_PASSWORD=change-me
